using FileReadWriteSample;
using System;
using System.IO;

class Program
{
    static void Main()
    {
        Console.WriteLine("Write and read a file sample");
        WriteAllLinesSample.WriteAllLinesSampleCode();
        ReadAllLinesSample.ReadAllLineSampleCode();

        AppendAllLinesSample.AppendAllLinesSampleCode();
        ReadAllLinesSample.ReadAllLineSampleCode();
    }
}
